# Dedicated mailbox that receives work request emails by BCC.

IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_SECURE=true
IMAP_USER=flexibag.schedule@gmail.com
IMAP_PASS=ulixeoukeabahvws
IMAP_MAILBOX=INBOX
POLL_SECONDS=180

# Optional: only parse messages from these sender domains or addresses.
# Leave empty to accept every sender.
ALLOWED_SENDERS=

# If true, marks processed emails as Seen after writing them to data files.
MARK_SEEN=false
